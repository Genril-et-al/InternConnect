/**
 * Shared application-wide constants.
 *
 * Import from here rather than scattering magic strings throughout the UI
 * so a single change propagates everywhere.
 */

/** Public contact email shown to users whenever they need to reach an admin. */
export const ADMIN_EMAIL = 'internconnect000@gmail.com'
